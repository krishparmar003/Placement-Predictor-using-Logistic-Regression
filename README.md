## Placement Predictor using Logistic Regression

A binary classification model that predicts whether a student will get placed based on their **CGPA** and **IQ score**. Built from scratch using scikit-learn with an ML pipeline.

## ML Pipeline

```
Raw Data → EDA → Feature Selection → Train-Test Split → Scaling → Training → Evaluation → Export
